# src
alias cdgit="cd ~/src/github"
alias cdiep="cd ~/src/iep-git"

# terraform
alias cdtf="cd ~/src/iep-git/iep-terraform"

# kube
alias k="kubectl"

# tools
[ -x "$(which bat)" ] && alias cat="bat"
[ -x "$(which batcat)" ] && alias cat="batcat"
